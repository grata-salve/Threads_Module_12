import utils.TimeFromStart;
import utils.ValuesReplace;

public class Main {
    public static void main(String[] args) {
        //TimeFromStart.countTimeFromStart();
        ValuesReplace.printNumbersWithReplace(15);
    }
}